# Go: Application Configuration Package

I often build applications that are required to pull configuration
from local or remote json data. This is a small utility package be used
with command line args or environment variables.